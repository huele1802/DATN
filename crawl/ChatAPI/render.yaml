services:
  - type: web
    name: plan-trip-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # bạn sẽ thêm sau trong Render dashboard
